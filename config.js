var angle = 30 * (Math.PI/180)//10-70//angle between branches
var brNumber = 2    // 2-5          //split into x branches
var brLenght = 170  // 100 - 300    //first branch height in px
var treeHeight = 12 // 12-15        //number of branches
var leafNumber = 5  // 2-6          //number of branches that are green
var brThick = 0.2   // 0.1 - 1      //thickness of branch
